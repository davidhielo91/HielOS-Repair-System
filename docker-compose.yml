version: "3.8"

services:
  sistema-taller:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - taller-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - ADMIN_PASSWORD=admin123
      - AUTH_SECRET=cambia-esto-por-algo-seguro
    restart: unless-stopped

volumes:
  taller-data:
